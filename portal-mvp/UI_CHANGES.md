## Ping 응답 형식 변경

- Ping 응답의 participant_id 제외. 내부 참여자 식별 및 다른 API 응답은 유지.
- Ping의 expires_at을 Unix 초에서 한국 시간 YYYYMMDDTHHMMSS+0900 문자열로 변경. lastPrivateInteractionTime과 같은 형식.
- 예: 1789722419.5180044 → 20260918T180659+0900.
- Web ECDIS에서는 expires_at * 1000 방식 대신 해당 문자열 형식에 맞게 처리해야 함.
- 서비스 이용 화면에 만료 후 참여 세션 재연결 및 서비스 재계약 안내 추가.
- 내부 만료 계산 및 다른 API의 expires_at은 변경하지 않음.

# Blue-X v0.9.0 — 서비스 연계 접속 정보

- 서비스 이용의 활성 계약에 서비스 접근 기본 URL, Ping URL, 허용된 데이터 경로별 URL을 표시함. 각각 표시/숨김 및 복사 기능 제공.
- 접속 키와 서비스 ID를 구분하고 접속 키는 기본 마스킹. 별도 컨수머 ID는 불필요함을 안내.
- 세션·계약 중 먼저 만료되는 시각을 표시. 기존 서버의 매 요청 권한 검사를 유지.
- Ping 테스트와 데이터 쿼리 테스트를 구분. 기존 날짜·영역·기상 쿼리를 그대로 입력 가능.
- 영어 기본 및 한국어 전환 지원. 모바일에서는 주소와 복사 버튼을 줄바꿈 배치.
- 성공 Ping 응답에 한국 시간의 lastPrivateInteractionTime 추가 (YYYYMMDDTHHMMSS+0900).
- 등록된 서비스의 허용 경로만 표시하며 griddata/gridfile을 임의 추가하지 않음.

배포: portal.py, console.html, assets/blue-x.js, assets/blue-x.css, assets/i18n.js를 함께 교체하고 서버 재시작. 기존 data/ 및 배포 환경 설정 보존.
브라우저에서 Web ECDIS가 직접 호출한다면 해당 Origin을 PORTAL_ALLOWED_ORIGINS에 허용해야 함.

검증: Python/JavaScript 문법 및 Node 렌더 검사(주소 생성, 이스케이프, 키 마스킹, 한영 문구, 허용 경로, 세션 만료 시 도구 숨김) 통과. 브라우저 설치 다운로드 실패로 실제 화면 캡처 검증은 수행하지 못함. pytest 미설치로 기존 Python 통합 테스트는 실행하지 못함.

---

# Blue-X v0.8.0 — English / 한국어

- 기본 UI는 영어입니다. 왼쪽 메뉴 상단의 Language 선택기로 English / 한국어를 전환합니다. 모바일에서는 상단 오른쪽에 표시됩니다.
- 선택 언어는 브라우저 localStorage에 저장됩니다. 새 브라우저 또는 저장값이 없으면 영어로 시작합니다.
- 로그인·회원가입, 참여자 개요, 자격증명, 서비스 제공/이용, 활동 로그, 운영 관리, 시스템 정보와 작업 확인창을 번역했습니다.
- 날짜는 영어 en-GB / 한국어 ko-KR 형식을 사용하며 시간대는 브라우저의 현지 시간대를 유지합니다.
- 언어를 바꿔도 작성 중인 입력값, 선택한 업로드 파일, 상세 펼침 상태와 데이터 요청 응답을 유지합니다. 처리 중에는 언어 변경을 잠시 비활성화합니다.
- 계정·조직·서비스 이름, 사용자 메모, URL과 API 응답 원문은 번역하지 않습니다. 서버 진단은 사전에 등록된 메시지만 번역하며 알 수 없는 상세 진단은 원문을 보존합니다. 저장된 감사 로그나 VC 내용은 변경하지 않습니다.
- assets/i18n.js에 두 언어의 UI 메시지와 언어 전환 로직이 있습니다. 신규 문구는 동일한 키의 en/ko 항목을 함께 추가합니다. HTML 조각의 동적 값은 기존 이스케이프 처리를 유지합니다.
- console.html이 영어 기본 HTML이며, 동일한 화면에서 새로고침 없이 두 언어로 전환합니다. 별도 HTML 복사본을 운영할 필요가 없습니다.
- 배포 시 assets/i18n.js를 포함한 assets 폴더 전체를 함께 옮겨주세요. 기존 data/ 폴더는 보존하세요.

## v0.8.0 검증

Chromium에서 영어 기본값, 한/영 전환, 새로고침 후 언어 유지, 입력값·파일 유지, 이름·API 응답 보존, 영어 세션 연결→계약→샘플 데이터 요청, 로그·운영자 화면 및 모바일 390px 배치를 확인했습니다. 테스트는 기존 VC 보고서 fixture 및 고정 샘플을 사용했습니다.

아래는 v0.7.0의 UI 변경 내역입니다.

# Blue-X v0.7.0 — UI 개선

## 주요 변경

- Blue-X / Trusted Data Transactions 브랜드 적용. BLUEMAP의 원본 로고와 B 심볼, 첨부 Gaia-X SVG의 색상·비율 유지.
- 첨부 Wizard의 인디고 #4F46E5, 바이올렛 #8B5CF6, 옅은 배경 #EEF2FF를 활용한 테마. 외부 폰트·CDN 없이 동작.
- 로그인·회원가입 화면 분리, 입력 검증 및 키보드 포커스 표시.
- 참여자 개요: 계정과 연결 조직 구분, 세션 만료·잔여 시간, 상태별 다음 작업, 검증→연결→제공/이용 진행 표시.
- 서비스 제공과 이용을 별도 메뉴로 구성. 서비스 이용은 서비스 찾기 / 내 이용 계약으로 분리.
- 계약·연결 상태와 국가 조건에 맞는 작업 버튼 표시. 최종 권한은 기존 서버 로직이 매 요청마다 판단.
- 계약 확인 안내, 계약 이후 주소 표시·복사·데이터 테스트. 원본 API 주소는 기존 접근 권한을 유지.
- 자격증명별 데모 연결 가능 여부, 현재 연결 사용 여부, 검사 상태별 건수, 문서별 상세 결과.
- 실패/확인 불가 검사가 있는 문서 상세는 상위 검증 결과를 펼쳤을 때 기본으로 열림.
- 제공 서비스 카드와 이용 참여자 목록. 기존 제공 서비스가 있으면 신규 등록 폼은 접어서 표시.
- 초기화·키 재발급을 개요의 접힌 관리 영역으로 이동. 파괴적 작업 확인 유지.
- 활동 로그의 기존 상세 정보·필터·페이지 이동·운영자 전체 조회 유지.
- 모바일 메뉴·카드·표 스크롤 대응. 시스템 정보 대화상자와 버전 표기.

## 실행·업데이트

기존과 같이 `python portal.py --port 8000`으로 실행합니다. 실행에는 Python 3.11 이상 및 requirements.txt의 의존성이 필요합니다.

`assets/` 폴더가 추가되었습니다. console.html만 옮기지 말고 프로젝트 파일과 assets/를 함께 배포하세요. Node.js와 프런트엔드 빌드 단계는 필요 없습니다.

기존 `data/` 폴더와 SQLite DB는 그대로 보존하세요. 이 배포 ZIP에는 계정·VC·테스트 DB가 없습니다. 기존 PORTAL_* 설정, API 경로, 접속 키, DB 스키마는 그대로 사용합니다. UI 개편으로 기존 계약이나 자격증명을 초기화하지 않습니다.

## 버전과 사양

- Blue-X 애플리케이션: 0.7.0
- 프로젝트 검증 프로필: loire-demo-v2 (이전 버전과 동일)
- 자격증명 환경: Loire / Non-production
- 설계 참고: Architecture Document 3.1, ICAM 25.11, Data Exchange Document 25.07

참고 사양과 실제 검증 프로필을 분리해 표시하며, 전체 사양 구현 또는 Gaia-X 적합성 인증을 의미하지 않습니다. Wizard의 2.3.0은 별도 도구 버전이므로 Blue-X 버전으로 표기하지 않습니다.

## 기존 파일

`dashboard.html`은 이전 검증기 화면의 보관 파일이며 현재 루트 경로는 `console.html`을 제공합니다. 데이터 검증 코드와 검증 정책은 수정하지 않았습니다.

## 검증 결과

- 기존 통합 테스트 26개 통과.
- Chromium에서 로그인 → VC 세션 연결 → 서비스 계약 → ping → 샘플 데이터 요청 → 서비스 등록을 확인.
- 자격증명 상세, 활동 로그, 운영자 화면, 시스템 정보 대화상자와 모바일 390px 화면을 확인.
- 테스트는 기존 테스트용 VC 검증 보고서 fixture 및 고정 샘플을 사용. 실제 VC의 암호 검증·외부 Weather API 연결을 새로 검증한 것은 아님.
- `previews/`의 화면은 테스트 계정·샘플 데이터로 촬영한 예시임. 실제 실행 시 사용자 계정과 데이터가 표시됨.
